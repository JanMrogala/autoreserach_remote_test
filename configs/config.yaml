model:
  arch: cnn
  channels: 32
  hidden_dim: 128
  num_layers: 2
  dropout: 0.1
  lr: 1e-3

training:
  max_epochs: 10
  batch_size: 64

wandb:
  project: "mnist-autoresearch"
  entity: null

save_artifacts:
  - "configs/config.yaml"
  - "model.py"
  - "train.py"
