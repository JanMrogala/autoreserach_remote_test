model:
  hidden_dim: 128
  num_layers: 2
  dropout: 0.1
  lr: 1e-3

training:
  max_epochs: 5
  batch_size: 64

wandb:
  project: ${oc.env:WANDB_PROJECT,mnist-autoresearch}
  entity: ${oc.env:WANDB_ENTITY,null}

save_artifacts:
  - "configs/config.yaml"
  - "model.py"
  - "train.py"
