model:
  hidden_dim: 256
  num_layers: 3
  dropout: 0.05
  lr: 5e-4

training:
  max_epochs: 10
  batch_size: 128

wandb:
  project: "mnist-autoresearch"
  entity: null

save_artifacts:
  - "configs/config.yaml"
  - "model.py"
  - "train.py"
